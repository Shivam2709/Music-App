/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'images.pexels.com', 'media.istockphoto.com']
    }
};

export default nextConfig;
